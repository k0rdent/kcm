---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: datasources.k0rdent.mirantis.com
spec:
  group: k0rdent.mirantis.com
  names:
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    shortNames:
    - ds
    singular: datasource
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: DataSource is the Schema for the datasources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceSpec defines the desired state of DataSource
            properties:
              auth:
                description: Auth provides authentication and optional TLS configuration
                  for connecting to the data source.
                properties:
                  caSecretKeyRef:
                    description: |-
                      CASecretKeyRef optionally references a Secret key containing the CA certificate used to verify
                      TLS connections to the data source.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  name:
                    description: Name is the username used when authenticating to
                      the data source.
                    type: string
                  passwordSecretKeyRef:
                    description: PasswordSecretKeyRef references a Secret key containing
                      the password for the username.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - name
                type: object
              endpoints:
                description: Endpoints contains one or more host/port pairs that clients
                  should use to connect to the data source.
                example: postgres-db1.example.com:5432
                items:
                  type: string
                type: array
              type:
                default: postgresql
                description: Type specifies the data service type (e.g. "postgresql")
                type: string
            required:
            - endpoints
            - type
            type: object
        type: object
    served: true
    storage: true

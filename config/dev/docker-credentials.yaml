apiVersion: v1
kind: Secret
metadata:
  name: docker-cluster-secret
  namespace: ${NAMESPACE}
  labels:
    k0rdent.mirantis.com/component: "kcm"
type: Opaque
---
apiVersion: k0rdent.mirantis.com/v1alpha1
kind: Credential
metadata:
  name: docker-cluster-cred
  namespace: ${NAMESPACE}
spec:
  description: Docker Credentials
  identityRef:
    apiVersion: v1
    kind: Secret
    name: docker-cluster-secret
    namespace: ${NAMESPACE}
